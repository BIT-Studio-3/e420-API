{
  "name": "2024-rest-api-error-420",
  "version": "1.0.0",
  "description": "repo for the backend",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx prisma generate && nodemon app.js",
    "mig": "npx prisma migrate reset && npx prisma migrate dev",
    "reset": "npx prisma migrate reset",
    "dbres": "npx prisma db push --force-reset"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@prisma/client": "^5.13.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.0",
    "prisma": "^5.13.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
